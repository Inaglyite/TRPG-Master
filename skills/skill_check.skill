# 技能/属性检定流程 (v2 — 强化 DC 遵守)

**引用 Skill：** 本 Skill 必须遵循 [[no_spoiler.skill]] 中的所有约束。

---

## 最重要的规则（违反即 GM 失格）

**你必须调用 skill_check() 进行技能检定。此函数自动读取 PC 属性、计算修正值、掷骰、比较 DC。**
**你绝不能手动掷骰然后心算修正值。所有数值计算由 skill_check() 完成。**
**检定失败 = 行动失败。检定成功才可揭示信息。**

---

## 自由行动确认规则

**当玩家以自由行动（而非预设选项）做出需要技能检定的动作时，必须先调用 suggest_check() 确认。**

流程：
1. 调用 `suggest_check(skill, attribute, dc, dc_label, description)` 告知玩家即将进行的检定
2. 如果返回 `confirmed: false` → 不要掷骰！改为描述玩家犹豫或重新生成选项
3. 如果返回 `confirmed: true` → 调用 skill_check()

**预设选项不需要确认，直接调 skill_check()。**
**自由行动且结果不确定 → 必须先 suggest_check 确认。**

---

## 检定流程（严格执行）

### 核心：一招搞定

调用 `skill_check(skill="investigation", dc=15)` 即可。该函数自动：

1. 读取 PC 的对应技能值和属性值
2. 查 rule_schema.json 中的属性修正表
3. 计算：属性修正 + 技能加值（技能值/10 向下取整）
4. 掷 d20
5. 比较最终值 vs DC
6. 检测大成功（自然20）和大失败（自然1）
7. 返回完整结果

**结果解读：**
- `success: true` → 成功，可揭示 TIER_1 信息，调用 state_add_clue()
- `success: false` → 失败，描述未能获取信息
- `critical: true` → 大成功！自动成功 + 额外叙事收益
- `fumble: true` → 大失败！自动失败 + 严重负面后果（考虑调用 sanity_loss）

### 优势/劣势

`skill_check(skill="stealth", dc=15, advantage="advantage")` 两骰取高
`skill_check(skill="athletics", dc=18, advantage="disadvantage")` 两骰取低

---

## 正确 vs 错误示例

### 示例 1：调查地毯污渍（DC 15，d20=3，修正=+7，最终=10）

```
错误: "你检查了地毯，发现了一块值得注意的污渍。"  ← 最终=10 < DC=15，不应发现
正确: "你蹲下身仔细查看地毯上的深色斑痕。烛光太暗，你只能判断这确实是某种液体留下的痕迹，
      但无法确定具体是什么。也许需要更近的光源——或者换个角度。"
      ← 失败了，给出向前的暗示但不给信息
```

### 示例 2：说服管家（DC 18，d20=20，修正=+2，最终=22）

```
自然 20！
正确: "你的话语似乎击中了管家内心深处的某根弦。老人颤抖的手忽然停住了，
      他抬起头——第一次真正看向你的眼睛。'先生，'他嘶哑地说，'有些事……
      我本不该说出口的。但您让我想起了三十年前的那个夜晚。'"
      ← 大成功，管家开始透露不触及 secret 的信息
```

### 示例 3：搜索暗门（DC 20，d20=16，修正=+4，最终=20）

```
恰好等于 DC=20
正确: "你的手指沿着书架的边缘缓缓滑过，感受到了一丝不自然的缝隙——
      这个书架不是固定死的。它后面有空间。"
      ← 刚好成功，简单陈述发现
```

---

## DC 参考表

| 难度 | DC | 示例 |
|------|-----|------|
| 琐碎 | 5 | 在明亮房间找一本显眼的书 |
| 简单 | 10 | 注意到地毯上有明显的大块污渍 |
| 中等 | 15 | 识别血迹与普通污渍的区别 |
| 困难 | 20 | 发现被巧妙隐藏的暗门 |
| 极难 | 25 | 破译一份残缺的加密手稿 |
| 近乎不可能 | 30 | 在完全黑暗中靠触觉识破陷阱 |

---

## 战斗检定（调用 combat.skill 规则）

攻击判定同样遵循 d20 + 修正 >= AC/DC 规则。命中后调用 apply_damage()，未命中则叙事描述失败。
