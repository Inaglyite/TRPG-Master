# 给 Kimi：后端、协议与 Agent 流程执行提示词

```text
请在实验分支实现“结构化操作跑团平台”的后端部分。
完整规格：docs/STRUCTURED_PLAY_PLATFORM_PLAN_20260913.md。
补充背景：docs/KEEPER_PLATFORM_HARNESS_DESIGN_20260913.md。
主规格优先；先完整阅读，不要只实现按钮对应的几个工具就结束。

产品原则：玩家按钮提交明确对象与操作的尝试请求；人类或 Agent
守秘人判断情境，调用公共领域命令。确定性服务负责合法性、骰点、
状态和持久化。新路径不得把结构请求拼成文字，再经正则重建动作。

你负责 src/、server.py、migrations/、schemas/、后端 tests、
协议文档/fixtures 与 Agent 提示词。zcode 负责 frontend/。
先查看 AGENTS.md、git status 和正在进行的修复；冻结实验基线，
保留他人修改，不自动提交他人的文件，不改真实存档或生产。

第一交付必须是 M0：
1. 新旧 execution_profile 与 human/assisted/agent 模式策略。
2. action_request、free_roll_request、check_response 和事件 schema。
3. action/check/command 状态机、错误码、权限矩阵和代表性 JSON fixtures。
4. 稳定物品/线索 ID 迁移与兼容方案。
5. 每命令提交与事件发布边界，具体如何避开旧整轮 turn_cache。
发布给前端的契约要落成可校验文件；字段变更同步维护，不口头约定。

之后依主规格 M1→M4 推进：
- 提取公共命令服务，旧 AI handler 可逐项委托，禁止万能工具 RPC。
- 实现结构请求的事实检查、持久队列/待办和主持处理。
- human 模式不用 Key、不建模型会话，keeper 不必占调查员。
- keeper 权限与 owner 分离，定向知识/图片在服务端过滤。
- 移动、线索、物品、HP/SAN、时间、发言均有明确命令和执行结果。
- 检定请求持久化；玩家点击才由服务端结算一次；普通骰不触发剧情。
- 状态、命令结果、恢复信息和待发布事件原子提交后才发权威结果。
  不仅检查同一个 store 的缓存值；用独立数据库连接做故障验证。
- 模型失败保留已提交命令，从游标继续，不重掷、不重扣、不重移动。
- Agent 实现判断→命令/叙事→读结果→继续/等待，按钮 ID 不擅改。
- “我想看看尸体”不能由关键词直接抵达；明确前往才移动；
  “那就过去”根据已约定目的地承接，歧义时澄清。
- 增加预算、无进展处理、控制权 epoch 与接管；迟到工具失效。
- 规则计算/权限/不变量保留，新模式关闭对应旧关键词自动执行，
  明确效果所有权，防止新旧通路双结算。

验收不能只用 mock：先离线命令与数据库集成，再与 zcode 的实际
三客户端 UI 跑 human 闭环，最后依适用授权做独立真实模型验证。
完成主规格第12节矩阵及项目要求，实际失败逐项报告，不改测试
输入掩盖拒行。不要自动发布，不把 M0 或单个工具完成当作全项目完成。

交付：分阶段完成项、冻结协议/fixtures、文件清单、测试证据、
迁移与恢复说明、给前端的联调说明、未解决问题。
```

