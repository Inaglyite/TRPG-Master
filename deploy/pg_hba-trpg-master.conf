# Add before broader host rules in pg_hba.conf.
local   trpg_master   trpg_app                              scram-sha-256
host    trpg_master   trpg_app   127.0.0.1/32               scram-sha-256
host    trpg_master   trpg_app   ::1/128                    reject
