limit_req_zone $binary_remote_addr zone=trpg_staging_auth:10m rate=10r/m;

server {
    listen 8443 ssl;
    listen [::]:8443 ssl;
    server_name _;

    ssl_certificate /etc/nginx/tls/trpg-master.crt;
    ssl_certificate_key /etc/nginx/tls/trpg-master.key;
    ssl_protocols TLSv1.2 TLSv1.3;

    client_max_body_size 64m;
    proxy_read_timeout 3600s;
    proxy_send_timeout 3600s;

    location / {
        proxy_pass http://127.0.0.1:8766;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header Upgrade $http_upgrade;
        # Use a literal here so this staging vhost can coexist with the
        # production vhost's global $connection_upgrade map.
        proxy_set_header Connection "upgrade";
        proxy_buffering off;
    }

    location ~ ^/api/auth/(login|register)$ {
        limit_req zone=trpg_staging_auth burst=10 nodelay;
        proxy_pass http://127.0.0.1:8766;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
    }
}
